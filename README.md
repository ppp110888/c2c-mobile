# C2C Mobile

uni-app client for the C2C platform.

Create an environment file for the target build based on `.env.example`. Production builds must use HTTPS and WSS URLs accepted by the target mini-program platform.

Open the project in HBuilderX and run or build the desired target. Backend gateway, media, and WebSocket URLs are centralized in `utils/config.js`.
